name: Deploy

on:
  # Run on every push to main/master.  The keystone and capstone libraries are
  # cached by submodule commit hash (see Cache steps below), so pushes that
  # only change Vue source skip the expensive Emscripten library builds and
  # just re-run emcc + npm run build — which is fast.
  push:
    branches:
      - master
      - main
  # Allow a manual full rebuild + deploy at any time.
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # peaceiris/actions-gh-pages needs write access to push gh-pages branch

    steps:
      - name: Checkout repository (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential

      # ── Emscripten (must come before keystone/capstone builds) ───────────────

      - name: Set up Emscripten
        uses: mymindstorm/setup-emsdk@v16

      # ── Keystone ─────────────────────────────────────────────────────────────

      - name: Get keystone submodule commit hash
        id: keystone-ref
        run: echo "hash=$(git -C third_party/keystone rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Cache keystone Emscripten build
        id: cache-keystone
        uses: actions/cache@v4
        with:
          path: third_party/keystone/build
          key: keystone-emscripten-${{ runner.os }}-${{ steps.keystone-ref.outputs.hash }}

      - name: Build keystone (Emscripten)
        if: steps.cache-keystone.outputs.cache-hit != 'true'
        run: |
          mkdir -p third_party/keystone/build
          cd third_party/keystone/build
          emcmake cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_SHARED_LIBS=OFF \
            -DBUILD_LIBS_ONLY=ON \
            -DLLVM_TARGETS_TO_BUILD="all" \
            -G "Unix Makefiles"
          emmake make -j$(nproc)

      # ── Capstone ─────────────────────────────────────────────────────────────

      - name: Get capstone submodule commit hash
        id: capstone-ref
        run: echo "hash=$(git -C third_party/capstone rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Cache capstone Emscripten build
        id: cache-capstone
        uses: actions/cache@v4
        with:
          path: third_party/capstone/build
          key: capstone-emscripten-${{ runner.os }}-${{ steps.capstone-ref.outputs.hash }}

      - name: Build capstone (Emscripten)
        if: steps.cache-capstone.outputs.cache-hit != 'true'
        run: |
          mkdir -p third_party/capstone/build
          cd third_party/capstone/build
          emcmake cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_SHARED_LIBS=OFF \
            -DCAPSTONE_BUILD_LEGACY_TESTS=OFF \
            -DCAPSTONE_BUILD_CSTEST=OFF \
            -DCAPSTONE_BUILD_CSTOOL=OFF \
            -G "Unix Makefiles"
          emmake make -j$(nproc)

      # ── Node / Vue ───────────────────────────────────────────────────────────

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: wasmasm/package-lock.json

      - name: Install npm dependencies
        run: npm ci --prefix wasmasm

      # ── Build ────────────────────────────────────────────────────────────────

      - name: Build WASM and Vue app
        run: make build

      # ── E2E tests ────────────────────────────────────────────────────────────
      # Run against the already-built dist/ directory before deploying.
      # If any test fails the deploy step is skipped.

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
        working-directory: wasmasm

      - name: Run E2E tests
        run: npm run test:e2e
        working-directory: wasmasm
        env:
          CI: true

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: wasmasm/playwright-report/
          retention-days: 7

      # ── Deploy ───────────────────────────────────────────────────────────────

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./wasmasm/dist
          cname: wasmasm.computer
